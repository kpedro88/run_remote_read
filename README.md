# Run Remote Read (RRR)

A simple repository to submit test jobs that run remote reads,
in order to test the stability and reliability of the AAA (any data, any time, anywhere) data federation using `xrootd`.

## Usage

```bash
condor_submit test.jdl
```
